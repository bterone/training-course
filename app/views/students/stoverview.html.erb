<div class="row">
    <div class="col-md-9 p-4 d-flex flex-column position-static">
        <h2 class="featurette-heading">Learn the ways of <%= @course.coursename %>!</h2>
        <p class="lead"><%= @course.coursedesc %></p>
    </div>

    <aside class ="col-md-3 sidebar p-4 d-flex flex-column position-static">
        <h4 class="font-italic">About</h4>
        <p class="mb-0">Etiam porta <em>sem malesuada magna</em> mollis euismod. 
        Cras mattis consectetur purus sit amet fermentum. Aenean lacinia bibendum nulla sed consectetur.</p>

        <hr class="featurette-divider">
        <% if groupfind(@course).first == nil %>
            <a class="btn btn-danger" href="#">No group assigned</a>
        <% else %>
            <%= link_to "Go to Class", stgroup_path(@course), :class => 'btn btn-info' %>
        <% end %>
    </aside>
</div>